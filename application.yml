server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sibimenonkenthottil/vidhyala-config.git
  application:
    name: config
eureka:
  client:
    register-with-eureka: true
    eureka-server-port: 8761
